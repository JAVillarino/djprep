[workspace]
members = ["djprep-cli", "djprep-core"]
default-members = ["djprep-cli"]
resolver = "2"

[profile.release]
lto = true
strip = true
panic = "abort"
codegen-units = 1

[profile.dev]
# Faster builds during development
opt-level = 0
debug = true
